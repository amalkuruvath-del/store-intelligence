# Pipeline / Computer Vision
# Install locally: pip install -r pipeline/requirements.txt
# These are NOT installed inside Docker — the ML pipeline runs on bare metal
# for maximum hardware acceleration (see CHOICES.md Decision 7).

opencv-python>=4.8.0
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0
scipy>=1.10.0
shapely>=2.0.0
psycopg2-binary>=2.9.0
requests>=2.28.0
Pillow>=9.0.0
